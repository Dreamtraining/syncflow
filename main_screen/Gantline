package com.example.syncflow

data class GanttLine(
    val habitName: String,
    val hour: Int,
    val minute: Int,
    val second: Int
) {
    init {
        require(habitName.isNotBlank()) { "Habit name must not be blank" }
        require(hour in 0..23) { "Hour must be 0-23" }
        require(minute in 0..59) { "Minute must be 0-59" }
        require(second in 0..59) { "Second must be 0-59" }
    }

    fun display(): String {
        return "Habit: $habitName at %02d:%02d:%02d".format(hour, minute, second)
    }
}
